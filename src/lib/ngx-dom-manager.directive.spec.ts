import { NgxDomManagerDirective } from './ngx-dom-manager.directive';

describe('NgxDomManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxDomManagerDirective();
    expect(directive).toBeTruthy();
  });
});
